---
phase: 01-chatbot-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - README.md
  - .env.example
  - docs/schema-contract.md
  - docs/architecture.md
  - scripts/smoke_index.py
autonomous: false
requirements: [BOOT-01, BOOT-02]
user_setup:
  - service: pinecone
    why: "Read-only retrieval from the live trade-reports index"
    env_vars:
      - name: PINECONE_READ_KEY
        source: "Pinecone Console -> Project -> API keys -> create a 'Reader' role key"
    dashboard_config:
      - task: "Confirm index 'trade-reports' exists and namespace 'trade' is populated by the producer"
        location: "Pinecone Console -> Indexes -> trade-reports"

must_haves:
  truths:
    - "The trading-chatbot/ repo exists with backend/, frontend/, docs/, plan/ skeleton"
    - "docs/schema-contract.md carries the read-only metadata field table verbatim"
    - "A live-index smoke opens trade-reports, runs describe_index_stats(), and confirms namespace 'trade' without errors"
  artifacts:
    - path: "docs/schema-contract.md"
      provides: "Read-only producer/consumer metadata contract"
      contains: "schema_version"
    - path: ".env.example"
      provides: "Env var template for backend config"
      contains: "PINECONE_READ_KEY"
    - path: "scripts/smoke_index.py"
      provides: "No-app-code live-index smoke"
      contains: "describe_index_stats"
  key_links:
    - from: "scripts/smoke_index.py"
      to: "Pinecone index trade-reports / namespace trade"
      via: "describe_index_stats()"
      pattern: "describe_index_stats"
---

<objective>
Slice 0: Stand up the new standalone `trading-chatbot/` repository skeleton, commit the
read-only schema contract documentation, and prove the live Pinecone `trade-reports` index is
reachable with a Reader key before any application code is written.

Purpose: Unblock all downstream slices and surface any upstream contract / access problems
immediately, while pinning the producer/consumer schema as a committed document.
Output: Repo skeleton, `docs/schema-contract.md`, `.env.example`, and a runnable live-index
smoke script that confirms namespace `trade` exists.
</objective>

## Artifacts this phase produces (this plan)

- Repo directory skeleton: `backend/`, `frontend/`, `docs/`, `plan/`, `scripts/`
- `docs/schema-contract.md` — full read-only metadata field table + read-key process
- `docs/architecture.md` — one-paragraph data-flow note (producer writes, consumer reads)
- `.env.example` — `PINECONE_READ_KEY`, `PINECONE_INDEX=trade-reports`, `PINECONE_NAMESPACE=trade`, `ANTHROPIC_API_KEY`, `DATABASE_URL=sqlite:///./chat.db`
- `scripts/smoke_index.py` — no-app-code live-index smoke (`describe_index_stats`)
- `.gitignore`, `README.md` skeleton

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-chatbot-mvp/01-CONTEXT.md

# Source spec (authoritative slice detail) — mirrored into the new repo at plan/trading-chatbot.md
@plan/trading-chatbot.md

# Upstream read-only contract (producer side, this ai-trading-claude repo)
@README.md
@scripts/trade_schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize repo skeleton + .env.example + gitignore</name>
  <files>.gitignore, README.md, .env.example, docs/architecture.md</files>
  <read_first>
    - plan/trading-chatbot.md (slice 0 "Files" + "Repository layout" section — the directory tree is authoritative)
    - .planning/phases/01-chatbot-mvp/01-CONTEXT.md (Repository & Layout locked decisions)
  </read_first>
  <action>
    Run `git init` in the new `trading-chatbot/` root and create the directory skeleton exactly as
    the source plan's "Repository layout": `backend/`, `frontend/`, `docs/`, `plan/`, plus a
    `scripts/` dir for the smoke. Create `.env.example` with keys PINECONE_READ_KEY (blank),
    PINECONE_INDEX=trade-reports, PINECONE_NAMESPACE=trade, ANTHROPIC_API_KEY (blank),
    DATABASE_URL=sqlite:///./chat.db, CORS_ORIGINS=http://localhost:3000. Add a `.gitignore`
    covering `.env`, `__pycache__/`, `.venv/`, `node_modules/`, `chat.db`, `.next/`. Write a short
    `README.md` stating this is the read-only Pinecone RAG consumer of the ai-trading-claude
    trade-reports index. Add `docs/architecture.md` with the unidirectional data-flow note
    (producer writes to Pinecone; chatbot backend reads only; frontend talks only to backend).
    Note: the frontend uses a separate env file `frontend/.env.local.example` carrying `NEXT_PUBLIC_API_BASE=http://localhost:8000` (the backend base URL) — that file is authored in slice 5 (01-06); flag it here so the backend URL is discoverable cold from the bootstrap plan.
    Do NOT commit a real `.env`.
  </action>
  <acceptance_criteria>
    - `trading-chatbot/` contains backend/, frontend/, docs/, plan/, scripts/ directories (verify: `ls -d backend frontend docs plan scripts`)
    - `.env.example` contains the literal tokens PINECONE_READ_KEY, PINECONE_INDEX, ANTHROPIC_API_KEY, DATABASE_URL (verify: `grep -v '^#' .env.example | grep -c -E 'PINECONE_READ_KEY|PINECONE_INDEX|ANTHROPIC_API_KEY|DATABASE_URL'` returns 4)
    - `.gitignore` ignores `.env` (verify: `grep -qx '.env' .gitignore || grep -q '^\.env$' .gitignore`)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write docs/schema-contract.md from the read-only contract</name>
  <files>docs/schema-contract.md</files>
  <read_first>
    - plan/trading-chatbot.md ("Upstream contract with ai-trading-claude (slice 0)" — the metadata field table is the source of truth)
    - README.md (this repo, "Consumer Integration" section — producer's declared contract)
    - scripts/trade_schemas.py (this repo — producer single source of truth for field names/enums/schema_version)
  </read_first>
  <action>
    Author `docs/schema-contract.md` mirroring the upstream contract verbatim: index name
    `trade-reports`, cloud/region `aws/us-east-1` (env-overridable), embedding model
    `llama-text-embed-v2`, namespace `trade`, and the ID scheme
    `<TICKER>:<TYPE>:<YYYYMMDD-HHMM>:<section-slug>:<n>` (note it is lexically sortable by recency).
    Reproduce the full required-metadata field table (schema_version, ticker, company, report_type,
    generated_at, generated_date, signal, grade, composite_score, the per-dimension scores noting
    risk_score is INVERTED, the OPTIONS fields iv_rank/strategy_outlook/recommended_strategy/
    position_bias, prices, catalysts, run_id, source_path, section, chunk_index). Record the
    read-only-key process (Pinecone Console -> Reader role -> PINECONE_READ_KEY) and the stability
    commitment: additive fields are safe, renames/deletions require a coordinated upstream change +
    schema_version bump; consumer validates schema_version on read and refuses unknown majors.
  </action>
  <acceptance_criteria>
    - `docs/schema-contract.md` contains schema_version, source_path, report_type, generated_date, run_id field rows (verify: `grep -c -E 'schema_version|source_path|report_type|generated_date|run_id' docs/schema-contract.md` >= 5)
    - Document notes risk_score is INVERTED (verify: `grep -qi 'risk_score' docs/schema-contract.md && grep -qi 'invert' docs/schema-contract.md`)
    - Document names the ID scheme and namespace 'trade' (verify: `grep -q 'trade-reports' docs/schema-contract.md && grep -q 'namespace' docs/schema-contract.md`)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-index smoke against trade-reports (read key)</name>
  <files>scripts/smoke_index.py</files>
  <read_first>
    - plan/trading-chatbot.md (slice 0 step 0.4 — describe_index_stats smoke; the Gate)
    - .planning/phases/01-chatbot-mvp/01-CONTEXT.md (Upstream contract + retrieval gotcha)
  </read_first>
  <what-built>
    A standalone `scripts/smoke_index.py` (no app code, no FastAPI) that reads PINECONE_READ_KEY +
    PINECONE_INDEX + PINECONE_NAMESPACE from the environment, imports `Pinecone`, opens the index,
    calls `describe_index_stats()`, and prints the namespace list and total vector count. The
    script must exit cleanly (skip with a clear message) when PINECONE_READ_KEY is absent so it is
    safe to run without the key.
  </what-built>
  <action>
    Implement `scripts/smoke_index.py` as described in what-built. It must distinguish three
    outcomes without raising: (a) namespace `trade` present with vector_count > 0 -> print the
    count; (b) namespace present but empty -> print a clean "namespace 'trade' empty" message
    (acceptable for slice 0); (c) missing key -> print "PINECONE_READ_KEY not set, skipping" and
    exit 0. The human runs it once with a real Reader key from the Pinecone Console.
  </action>
  <how-to-verify>
    1. Obtain a Reader-role key from Pinecone Console -> Project -> API keys.
    2. Export it: set PINECONE_READ_KEY in your shell (or a local .env you do not commit).
    3. Run `python scripts/smoke_index.py` (after `pip install pinecone>=5`).
    4. Confirm output lists namespace `trade` and a vector count (>= 0) with NO errors/tracebacks.
  </how-to-verify>
  <acceptance_criteria>
    - Smoke prints namespace `trade` and a numeric vector_count, or a clean "namespace empty" line, with no traceback (slice 0 Gate)
    - Read key works (no auth error)
    - With no key set, the script prints a skip message and exits 0
  </acceptance_criteria>
  <resume-signal>Type "approved" once the smoke shows namespace 'trade' and a vector count with no errors, or describe the error seen.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chatbot -> Pinecone | Read-only key crosses to a third-party vector DB |
| local env -> repo | Secrets (.env) must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env / read key | mitigate | `.gitignore` excludes `.env`; only `.env.example` (blank values) committed; key is Reader-role only |
| T-01-02 | Elevation of Privilege | Pinecone access | mitigate | Reader-role key only — chatbot is consumer-only, never holds the producer write key |
| T-01-SC | Tampering | pip install pinecone | mitigate | Single mainstream package `pinecone>=5` pinned per CONTEXT.md locked deps; verify on pypi.org/project/pinecone before install |
</threat_model>

<verification>
- Repo skeleton + docs committed; `git status` clean after the slice-0 commit.
- `docs/schema-contract.md` reproduces the full metadata table.
- Live-index smoke confirms namespace `trade` reachable with the Reader key (Gate).
</verification>

<success_criteria>
- BOOT-01: `trading-chatbot/` repo exists with backend/frontend/docs/plan skeleton and a committed `docs/schema-contract.md`.
- BOOT-02: Live-index smoke opens `trade-reports`, runs `describe_index_stats()`, confirms namespace `trade` without errors.
</success_criteria>

<output>
Create `.planning/phases/01-chatbot-mvp/01-01-SUMMARY.md` when done.
</output>
