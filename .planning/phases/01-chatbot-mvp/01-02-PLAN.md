---
phase: 01-chatbot-mvp
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/pyproject.toml
  - backend/.python-version
  - backend/src/__init__.py
  - backend/src/config.py
  - backend/src/main.py
  - backend/src/pinecone_client.py
  - backend/src/routes/__init__.py
  - backend/src/routes/health.py
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_pinecone_client.py
autonomous: true
requirements: [RAG-01, RAG-02]
user_setup: []

must_haves:
  truths:
    - "/readyz returns 200 with a real Pinecone vector_count"
    - "/healthz returns {status:'ok'}"
    - "backend pytest passes"
    - "retrieve('AAPL', k=3) returns normalized chunks from the live index"
  artifacts:
    - path: "backend/src/pinecone_client.py"
      provides: "retrieve / latest / timeline retrieval primitives + _normalize"
      contains: "def retrieve"
    - path: "backend/src/config.py"
      provides: "Pydantic Settings bound to env"
      contains: "pinecone_read_key"
    - path: "backend/src/routes/health.py"
      provides: "/healthz and /readyz routes"
      contains: "readyz"
    - path: "backend/src/main.py"
      provides: "FastAPI app + CORS + health router"
      contains: "FastAPI"
  key_links:
    - from: "backend/src/routes/health.py"
      to: "Pinecone describe_index_stats()"
      via: "/readyz handler"
      pattern: "describe_index_stats"
    - from: "backend/src/pinecone_client.py"
      to: "settings.pinecone_index / namespace"
      via: "index handle"
      pattern: "pinecone_index|pinecone_namespace"
---

<objective>
Slice 1: Build the Python/FastAPI backend skeleton and the read-only Pinecone retrieval client,
exposing `/healthz` and `/readyz`. This is the data-access foundation every later slice consumes.

Purpose: Provide normalized retrieval primitives (`retrieve`/`latest`/`timeline`) and prove live
Pinecone reachability through `/readyz`.
Output: A runnable FastAPI app, a tested `pinecone_client`, and passing pytest.
</objective>

## Artifacts this phase produces (this plan)

- `backend/pyproject.toml` (uv-managed deps), `backend/.python-version` (3.12)
- `backend/src/config.py` -> `Settings` (`pinecone_read_key`, `pinecone_index`, `pinecone_namespace`, `anthropic_api_key`, `anthropic_model`, `database_url`, `cors_origins`) + `settings` instance
- `backend/src/pinecone_client.py` -> `retrieve(text, ticker?, report_type?, k=5)`, `latest(ticker, report_type="ANALYSIS")`, `timeline(ticker, limit=20)`, `_normalize(v)`
- `backend/src/main.py` -> FastAPI `app` with CORS + health router
- `backend/src/routes/health.py` -> routes `/healthz`, `/readyz`
- `backend/tests/test_pinecone_client.py`, `backend/tests/conftest.py`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-chatbot-mvp/01-CONTEXT.md
@plan/trading-chatbot.md
@.planning/phases/01-chatbot-mvp/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffolding — pyproject, config, main app, CORS, health router</name>
  <files>backend/pyproject.toml, backend/.python-version, backend/src/__init__.py, backend/src/config.py, backend/src/main.py, backend/src/routes/__init__.py, backend/src/routes/health.py</files>
  <read_first>
    - plan/trading-chatbot.md (slice 1 steps 1.1, 1.2, 1.4, 1.5 — exact pyproject deps + config fields + health routes)
    - .planning/phases/01-chatbot-mvp/01-CONTEXT.md (Backend stack locked deps + config.py field defaults)
  </read_first>
  <action>
    Write `backend/pyproject.toml` for an uv-managed project named trading-chatbot-backend,
    requires-python >=3.12, with the locked dependencies: fastapi>=0.115, uvicorn[standard]>=0.32,
    pinecone>=5, anthropic>=0.40, yfinance>=0.2.40, pydantic>=2.9, pydantic-settings>=2.6,
    sqlmodel>=0.0.22, sse-starlette>=2.1, httpx>=0.27; dev-deps pytest>=8, pytest-asyncio>=0.24,
    respx>=0.21. Write `.python-version` = 3.12. Implement `config.py` as a pydantic-settings
    `Settings` class with fields pinecone_read_key, pinecone_index="trade-reports",
    pinecone_namespace="trade", anthropic_api_key, anthropic_model, database_url="sqlite:///./chat.db",
    cors_origins=["http://localhost:3000"], reading from `.env`, and export a module-level `settings`.
    For `anthropic_model`: do NOT hardcode the source plan's possibly-stale `claude-opus-4-7`;
    confirm a current Claude model id at implementation time (claude-api skill) and use that as the
    default. Implement `main.py` creating the FastAPI `app`, adding CORSMiddleware from
    settings.cors_origins, and including the health router. Implement `routes/health.py` with an
    APIRouter exposing GET `/healthz` returning {status:"ok"} and GET `/readyz` that opens the
    Pinecone index and returns its `describe_index_stats()` vector count (200 on success; 503 with a
    graceful "memory unavailable" body on Pinecone error).
  </action>
  <acceptance_criteria>
    - `config.py` defines Settings with pinecone_read_key and anthropic_model fields (verify: `grep -c -E 'pinecone_read_key|anthropic_model' backend/src/config.py` >= 2)
    - `routes/health.py` exposes both healthz and readyz (verify: `grep -q 'healthz' backend/src/routes/health.py && grep -q 'readyz' backend/src/routes/health.py`)
    - anthropic_model default is NOT the stale string `claude-opus-4-7` (verify: `! grep -q 'claude-opus-4-7' backend/src/config.py`)
    - pyproject lists pinecone, anthropic, fastapi, sse-starlette (verify: `grep -c -E 'pinecone|anthropic|fastapi|sse-starlette' backend/pyproject.toml` >= 4)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Pinecone retrieval client — retrieve / latest / timeline / _normalize</name>
  <files>backend/src/pinecone_client.py</files>
  <read_first>
    - plan/trading-chatbot.md (slice 1 step 1.3 — the three retrieval primitives + _normalize)
    - .planning/phases/01-chatbot-mvp/01-CONTEXT.md (Retrieval primitives + the "retrieval gotcha" on metadata filters)
    - docs/schema-contract.md (metadata field names returned in each chunk)
  </read_first>
  <action>
    Implement `pinecone_client.py` against the live `trade-reports` index in namespace
    `settings.pinecone_namespace`. Expose three primitives, each returning a list (or dict/None for
    latest) of normalized chunks via `_normalize(v)` which flattens a Pinecone match into
    {id, score, text, metadata}: `retrieve(text, ticker=None, report_type=None, k=5)` does semantic
    search (integrated-inference query against the index) with optional metadata filter — but treat
    metadata $eq/$in filters as best-effort/unreliable per the producer gotcha, so apply
    ticker/report_type as a post-filter on returned matches when the server-side filter is empty;
    `latest(ticker, report_type="ANALYSIS")` lists IDs by prefix `<TICKER>:<TYPE>:` and fetches the
    lexically-greatest (newest) match, returning the normalized chunk or None; `timeline(ticker,
    limit=20)` lists IDs by prefix `<TICKER>:`, sorts newest-first by the lexically-sortable ID, and
    returns up to `limit` normalized chunks. Validate `schema_version` on read and refuse unknown
    majors (currently 1). Prefer ID-prefix list/fetch for latest/timeline over metadata filters.
  </action>
  <acceptance_criteria>
    - `pinecone_client.py` defines retrieve, latest, timeline, and _normalize (verify: `grep -c -E 'def (retrieve|latest|timeline|_normalize)' backend/src/pinecone_client.py` == 4)
    - Uses ID-prefix listing for latest/timeline (verify: `grep -qi 'prefix' backend/src/pinecone_client.py`)
    - _normalize returns id/score/text/metadata shape (verify: `grep -q 'metadata' backend/src/pinecone_client.py`)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Live-index tests + pytest + /readyz verification</name>
  <files>backend/tests/__init__.py, backend/tests/conftest.py, backend/tests/test_pinecone_client.py</files>
  <read_first>
    - plan/trading-chatbot.md (slice 1 steps 1.6–1.8 + the Gate)
    - backend/src/pinecone_client.py (the functions under test)
    - .planning/phases/01-chatbot-mvp/01-CONTEXT.md (Claude's Discretion — live-index smokes skippable when key absent)
  </read_first>
  <action>
    Write `tests/test_pinecone_client.py` with three live-index smoke tests asserting `retrieve`
    returns a list, `latest` returns a dict-or-None, and `timeline` returns a list. In
    `conftest.py`, add a fixture/marker that skips these tests cleanly when PINECONE_READ_KEY is
    absent (pytest.skip), so the suite is green without the key but exercises the live index when it
    is present. Then run `uv venv && uv pip sync` (or `uv pip install`) and `uv run pytest` in
    backend/, and start uvicorn with --reload to curl `/readyz` and confirm a real vector_count.
  </action>
  <acceptance_criteria>
    - `uv run pytest` passes in backend/ (slice 1 Gate — pytest passes)
    - `/readyz` returns HTTP 200 with a numeric vector_count from the live index (verify: `curl -s -o /dev/null -w '%{http_code}' localhost:8000/readyz` == 200 with key present)
    - `/healthz` returns {"status":"ok"} (verify: `curl -s localhost:8000/healthz | grep -q '"ok"'`)
    - Tests skip cleanly (not fail) when PINECONE_READ_KEY is unset
    - A REPL `retrieve("AAPL", k=3)` returns chunks when the key is present (slice 1 Gate)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> backend API | Untrusted HTTP requests reach /healthz, /readyz |
| backend -> Pinecone | Read-only key + retrieved untrusted report text crosses in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | /readyz error body | mitigate | On Pinecone failure return a generic "memory unavailable" 503 — do not leak keys/stack traces |
| T-02-02 | Tampering | schema drift on read | mitigate | Validate schema_version on read; refuse unknown majors |
| T-02-03 | Denial of Service | unbounded retrieve k | accept | Single-user MVP; k capped at small defaults (k<=6); rate limiting deferred to Phase 2 (RATE-01) |
| T-02-SC | Tampering | uv pip install deps | mitigate | All deps mainstream + version-pinned per CONTEXT.md locked stack; verify pinecone/fastapi/anthropic on pypi before install |
</threat_model>

<verification>
- `uv run pytest` green in backend/.
- `/readyz` 200 with real vector_count; `/healthz` returns ok.
- `retrieve`/`latest`/`timeline` return normalized {id,score,text,metadata} chunks.
</verification>

<success_criteria>
- RAG-01: `pinecone_client` exposes `retrieve`, `latest`, `timeline`, each returning normalized chunks.
- RAG-02: `/readyz` returns 200 with a real `vector_count`; `/healthz` returns {status:"ok"}; backend pytest passes.
</success_criteria>

<output>
Create `.planning/phases/01-chatbot-mvp/01-02-SUMMARY.md` when done.
</output>
