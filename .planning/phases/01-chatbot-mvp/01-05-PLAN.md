---
phase: 01-chatbot-mvp
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - backend/src/llm_client.py
  - backend/src/routes/chat.py
autonomous: true
requirements: [STREAM-01]
user_setup: []

must_haves:
  truths:
    - "POST /chat/stream emits SSE events in order: session -> citations -> repeated token -> done"
    - "Tokens arrive incrementally over time, not all at once"
    - "On completion both user and assistant turns are persisted"
  artifacts:
    - path: "backend/src/llm_client.py"
      provides: "stream_complete generator over OpenAI token stream"
      contains: "def stream_complete"
    - path: "backend/src/routes/chat.py"
      provides: "POST /chat/stream SSE endpoint"
      contains: "/chat/stream"
  key_links:
    - from: "backend/src/routes/chat.py"
      to: "llm_client.stream_complete"
      via: "SSE token generator"
      pattern: "stream_complete"
    - from: "backend/src/routes/chat.py"
      to: "session_store.append_turn"
      via: "persist on done"
      pattern: "append_turn"
---

<objective>
Slice 4: Add SSE streaming so the assistant response arrives token-by-token. Add a
`stream_complete` generator to the LLM wrapper and a `POST /chat/stream` endpoint emitting ordered
SSE events, buffering the full response to persist both turns on completion.

Purpose: Real-time token streaming — the responsiveness the frontend (slice 5) consumes.
Output: A `/chat/stream` endpoint emitting session -> citations -> token* -> done.
</objective>

## Artifacts this phase produces (this plan)

- Updated `backend/src/llm_client.py` -> `stream_complete(system, messages)` generator yielding text tokens from OpenAI's `chat.completions.create(..., stream=True)` deltas (`chunk.choices[0].delta.content`)
- Updated `backend/src/routes/chat.py` -> route `POST /chat/stream` (sse_starlette `EventSourceResponse`) emitting events `session`, `citations`, repeated `token`, `done`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-chatbot-mvp/01-CONTEXT.md
@plan/trading-chatbot.md
@.planning/phases/01-chatbot-mvp/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: stream_complete generator on the OpenAI wrapper</name>
  <files>backend/src/llm_client.py</files>
  <read_first>
    - plan/trading-chatbot.md (slice 4 step 4.1 — generator over the OpenAI streaming deltas)
    - backend/src/llm_client.py (existing complete() from slice 2)
    - backend/src/config.py (openai_model)
  </read_first>
  <action>
    Add `stream_complete(system, messages)` to `llm_client.py` — a generator that calls the OpenAI
    Chat Completions API with `stream=True` using settings.openai_model, the system
    prompt prepended as a `{"role":"system",...}` message, the messages, and max_tokens=2048, and yields
    each non-empty `chunk.choices[0].delta.content`. Keep
    `complete()` unchanged. On provider error, surface a graceful "LLM provider unavailable"
    condition the route can convert into a terminating SSE error/done.
  </action>
  <acceptance_criteria>
    - llm_client defines stream_complete and retains complete (verify: `grep -q 'def stream_complete' backend/src/llm_client.py && grep -q 'def complete' backend/src/llm_client.py`)
    - Uses the OpenAI streaming API with stream=True (verify: `grep -qi 'stream' backend/src/llm_client.py`)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: POST /chat/stream SSE endpoint + curl -N smoke</name>
  <files>backend/src/routes/chat.py</files>
  <read_first>
    - plan/trading-chatbot.md (slice 4 steps 4.2, 4.3 + the Gate)
    - backend/src/routes/chat.py (existing /chat + history wiring from slice 4 wave)
    - backend/src/session_store.py (append_turn)
    - .planning/phases/01-chatbot-mvp/01-CONTEXT.md (Streaming locked: event order)
  </read_first>
  <action>
    Add `POST /chat/stream` in `routes/chat.py` using sse_starlette `EventSourceResponse`. Resolve
    session_id, retrieve chunks (k=6), build the RAG prompt + history messages exactly as `/chat`.
    Emit, in order: `event: session` carrying the session_id; `event: citations` carrying the JSON
    list of Citations (once, up front); then `event: token` per token yielded by
    `stream_complete`, while buffering the full assistant text; on stream completion
    `append_turn` for both the user and assistant turns, then emit `event: done`. Citations must
    arrive once at the start, before any token. Then run the curl -N smoke.
  </action>
  <acceptance_criteria>
    - /chat/stream emits session, citations, token, done events in that order (verify: `grep -c -E 'session|citations|token|done' backend/src/routes/chat.py` covers all four event names)
    - Both turns persisted on completion (verify: `grep -c 'append_turn' backend/src/routes/chat.py` >= 2 in the stream handler)
    - `curl -N -X POST localhost:8000/chat/stream` shows `event: token` lines arriving over multiple seconds, not all at once; citations arrive once at the start (slice 4 Gate)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /chat/stream | Untrusted message opens a long-lived SSE stream |
| backend -> OpenAI | Streamed tokens cross from the provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | long-lived SSE connections | accept | Single-user MVP; connection caps + rate limiting deferred to Phase 2 (RATE-01) |
| T-05-02 | Information Disclosure | mid-stream provider error | mitigate | Emit a terminating graceful error then `done` — no key/stack leakage in the SSE payload |
| T-05-03 | Tampering | injection in streamed citations JSON | mitigate | Citations serialized from typed Citation models built from real chunk metadata |
</threat_model>

<verification>
- `curl -N` shows incremental token events over multiple seconds; citations first.
- Both turns persisted after stream completes (verify via GET /sessions/{id}).
</verification>

<success_criteria>
- STREAM-01: POST /chat/stream emits SSE events session -> citations -> repeated token -> done; tokens arrive incrementally over time, not all at once.
</success_criteria>

<output>
Create `.planning/phases/01-chatbot-mvp/01-05-SUMMARY.md` when done.
</output>
