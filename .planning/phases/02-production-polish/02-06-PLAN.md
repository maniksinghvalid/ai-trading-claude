---
phase: 02-production-polish
plan: 06
type: execute
wave: 5
depends_on: ["02-02", "02-03"]
files_modified:
  - trading-chatbot/frontend/components/SessionList.tsx
  - trading-chatbot/frontend/components/CitationCard.tsx
  - trading-chatbot/frontend/components/QuoteCard.tsx
  - trading-chatbot/frontend/components/TickerChip.tsx
  - trading-chatbot/frontend/components/StreamingMarkdown.tsx
  - trading-chatbot/frontend/components/ChatWindow.tsx
  - trading-chatbot/frontend/components/MessageBubble.tsx
  - trading-chatbot/frontend/app/layout.tsx
  - trading-chatbot/frontend/app/page.tsx
  - trading-chatbot/frontend/lib/api.ts
  - trading-chatbot/frontend/lib/types.ts
autonomous: false
requirements: [POLISH-01]
must_haves:
  truths:
    - "A sidebar lists prior sessions (GET /sessions); clicking one restores full history (GET /sessions/{id})"
    - "Citation cards expand to show chunk text"
    - "Ticker chips highlight detected tickers in messages"
    - "A live quote renders in a distinct QuoteCard, separate from cited memory"
    - "Refresh → sessions visible in sidebar → click → full history restored"
  artifacts:
    - path: "trading-chatbot/frontend/components/SessionList.tsx"
      provides: "Sidebar session list fetching GET /sessions, click loads GET /sessions/{id}"
      contains: "sessions"
    - path: "trading-chatbot/frontend/components/QuoteCard.tsx"
      provides: "Live quote card distinct from citations"
      contains: "QuoteCard"
  key_links:
    - from: "trading-chatbot/frontend/components/SessionList.tsx"
      to: "/sessions"
      via: "fetch GET /sessions with Authorization Bearer; click → GET /sessions/{id}"
      pattern: "/sessions"
    - from: "trading-chatbot/frontend/components/QuoteCard.tsx"
      to: "event: quote"
      via: "ChatWindow parses the SSE quote event into a QuoteCard"
      pattern: "quote"
---

<objective>
Polish the frontend (slice 11, POLISH-01): a sidebar session list (GET /sessions) whose entries
load full history (GET /sessions/{id}), expandable citation cards, ticker chips highlighting
detected tickers, a distinct QuoteCard for live quotes (from 02-02's SSE quote event), and smooth
streaming via incremental (debounced) markdown rendering.

Purpose: Phase 1's UI keeps sessionId in React state only (lost on reload). This plan adds
cross-reload session persistence in the UI, surfaces citations richly, and renders the new live
quote distinctly from cited memory — the visible payoff of slices 6–8.

Output: SessionList, CitationCard, QuoteCard, TickerChip, StreamingMarkdown components wired into
the layout + ChatWindow, plus the api.ts helpers for the new endpoints/events.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-production-polish/02-CONTEXT.md
@trading-chatbot/frontend/components/ChatWindow.tsx
@trading-chatbot/frontend/components/MessageBubble.tsx
@trading-chatbot/frontend/lib/api.ts
@trading-chatbot/frontend/lib/types.ts
@trading-chatbot/frontend/app/layout.tsx
@trading-chatbot/frontend/app/page.tsx
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan:
- `frontend/components/SessionList.tsx` — sidebar fetching `GET /sessions` (with Bearer), rendering session titles; onClick fetches `GET /sessions/{id}` and loads its turns into ChatWindow.
- `frontend/components/CitationCard.tsx` — replaces the inline Sources list; expandable to show chunk text (collapsed by default).
- `frontend/components/QuoteCard.tsx` — renders {price, day_change_pct, volume, timestamp, source} distinctly from citations; shows the ~15-min-delayed note.
- `frontend/components/TickerChip.tsx` — highlights detected ticker symbols in message content.
- `frontend/components/StreamingMarkdown.tsx` — debounced incremental markdown rendering for smooth token streaming (wraps the safe ReactMarkdown config from MessageBubble — no rehype-raw, preserving T-06-01).
- `lib/api.ts` additions: `fetchSessions()` (GET /sessions), `fetchSessionTurns(id)` (GET /sessions/{id}), both sending the Bearer token; `streamChat` now also yields a `quote` event.
- `lib/types.ts` additions: `Quote` interface; `SessionSummary` interface; `StreamEvent.event` union extended with `"quote"`.
- `app/layout.tsx` + `app/page.tsx`: two-pane layout with `<SessionList/>` sidebar + `<ChatWindow/>`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: SessionList sidebar + session-history loading + api.ts/types additions</name>
  <files>trading-chatbot/frontend/components/SessionList.tsx, trading-chatbot/frontend/lib/api.ts, trading-chatbot/frontend/lib/types.ts, trading-chatbot/frontend/app/layout.tsx, trading-chatbot/frontend/app/page.tsx, trading-chatbot/frontend/components/ChatWindow.tsx</files>
  <read_first>
    - trading-chatbot/frontend/lib/api.ts (streamChat fetch + Bearer header from 02-03; mirror the header logic)
    - trading-chatbot/frontend/lib/types.ts (Citation/Message/StreamEvent shapes)
    - trading-chatbot/frontend/components/ChatWindow.tsx (messages/sessionId state; where to inject loaded history)
    - trading-chatbot/backend/src/routes/sessions.py (GET /sessions + GET /sessions/{id} response shapes)
    - .planning/phases/02-production-polish/02-CONTEXT.md (slice 11 locked: sidebar, click-to-load history)
  </read_first>
  <action>
    Add `fetchSessions()` and `fetchSessionTurns(id)` to `lib/api.ts` (GET the backend, attach
    `Authorization: Bearer <access_token>` from localStorage like streamChat). Add `SessionSummary`
    ({session_id, title}) and `Quote` interfaces + extend `StreamEvent.event` with `"quote"` in
    `lib/types.ts`. Create `SessionList.tsx` ("use client"): on mount calls fetchSessions, renders
    titles; clicking a session calls fetchSessionTurns and invokes an `onSelectSession(sessionId,
    turns)` callback prop. Restructure `app/layout.tsx`/`app/page.tsx` into a two-pane layout
    (sidebar + chat). In `ChatWindow.tsx`, accept the selected session: set sessionId and map the
    loaded turns into the `messages` state (role/content), so clicking a session restores its full
    history. Refreshing the page re-fetches the session list (cross-reload persistence at the UI
    level).
  </action>
  <verify>
    <automated>cd trading-chatbot/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - SessionList fetches GET /sessions with a Bearer header and renders session titles.
    - Clicking a session fetches GET /sessions/{id} and ChatWindow renders the restored turns.
    - `npm run build` succeeds with no type errors.
  </acceptance_criteria>
  <done>The sidebar lists prior sessions and clicking one restores full history into the chat pane.</done>
</task>

<task type="auto">
  <name>Task 2: CitationCard (expandable), QuoteCard, TickerChip</name>
  <files>trading-chatbot/frontend/components/CitationCard.tsx, trading-chatbot/frontend/components/QuoteCard.tsx, trading-chatbot/frontend/components/TickerChip.tsx, trading-chatbot/frontend/components/MessageBubble.tsx, trading-chatbot/frontend/components/ChatWindow.tsx, trading-chatbot/frontend/lib/types.ts</files>
  <read_first>
    - trading-chatbot/frontend/components/MessageBubble.tsx (existing Sources list + T-06-01 safe-render constraints — preserve no-rehype-raw)
    - trading-chatbot/frontend/lib/types.ts (Citation; new Quote from Task 1)
    - .planning/phases/02-production-polish/02-CONTEXT.md (slice 11 locked: expandable citation cards, ticker chips, distinct quote card)
    - .planning/phases/02-production-polish/02-02-PLAN.md (SSE `event: quote` payload shape)
  </read_first>
  <action>
    Create `CitationCard.tsx`: renders a citation collapsed (source_path • report_type •
    generated_date) with an expand toggle revealing the chunk text when present; replace the inline
    Sources list in `MessageBubble.tsx` with a list of CitationCards. Create `QuoteCard.tsx`:
    renders {price, day_change_pct (green/red), volume, timestamp, source} with a small "~15 min
    delayed" note; styled distinctly from citation cards. Create `TickerChip.tsx`: a small pill
    highlighting a ticker symbol. In `ChatWindow.tsx`, handle the new SSE `quote` event by storing
    a quote on the current assistant message and rendering a `<QuoteCard/>` above the bubble text;
    detect tickers in assistant content (simple uppercase-symbol match against the message) and
    wrap them in `<TickerChip/>`. Keep all rendering through the safe ReactMarkdown path (no
    rehype-raw) — T-06-01.
  </action>
  <verify>
    <automated>cd trading-chatbot/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Citations render as CitationCards that expand to show chunk text.
    - A `quote` SSE event renders a QuoteCard distinct from citations, showing price/timestamp/source + the delay note.
    - Ticker chips highlight detected symbols; no raw-HTML rendering path is introduced (T-06-01 preserved).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Citation cards expand, the live quote renders in a distinct QuoteCard, and ticker chips highlight detected symbols.</done>
</task>

<task type="auto">
  <name>Task 3: StreamingMarkdown — debounced incremental rendering</name>
  <files>trading-chatbot/frontend/components/StreamingMarkdown.tsx, trading-chatbot/frontend/components/MessageBubble.tsx</files>
  <read_first>
    - trading-chatbot/frontend/components/MessageBubble.tsx (current ReactMarkdown config + component overrides to reuse)
    - .planning/phases/02-production-polish/02-CONTEXT.md (slice 11 locked: incremental markdown, debounced parse)
  </read_first>
  <action>
    Create `StreamingMarkdown.tsx` ("use client"): wraps the existing safe ReactMarkdown config
    (anchor/code overrides, NO rehype-raw — T-06-01) and debounces re-parsing of the streaming
    content (e.g. ~80ms) so rapid token updates don't thrash the markdown parser, while still
    flushing the final content on stream completion. Use it in `MessageBubble.tsx` for the
    assistant message body in place of the direct ReactMarkdown call. Ensure the final rendered
    text exactly equals the full streamed content (no dropped trailing tokens).
  </action>
  <verify>
    <automated>cd trading-chatbot/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Assistant messages render through StreamingMarkdown with debounced parsing.
    - The final rendered content equals the complete streamed text (no truncation).
    - No rehype-raw / dangerouslySetInnerHTML is introduced (T-06-01 preserved).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Streaming markdown renders smoothly via debounced parsing while preserving the safe-render security posture.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify polished frontend end-to-end</name>
  <action>Pause for human end-to-end verification of the polished frontend against the running backend (no code runs in this task). See how-to-verify for the steps.</action>
  <what-built>The full polished frontend: sidebar session list, click-to-restore history, expandable citation cards, distinct QuoteCard, ticker chips, and smooth streaming. Backend (02-01..02-05) is running; user is logged in via magic link.</what-built>
  <how-to-verify>
    1. Run backend (`cd trading-chatbot/backend && uv run uvicorn src.main:app --reload`) and frontend (`cd trading-chatbot/frontend && npm run dev`); log in via the magic link.
    2. Ask "bull case for AAPL" → streamed answer with expandable citation cards.
    3. Ask "what's AAPL trading at?" → a distinct QuoteCard with price + timestamp + ~15-min note appears (separate from citations).
    4. Start a second session ("compare NVDA and AMD") → ticker chips highlight NVDA/AMD.
    5. Refresh the page → the sidebar lists both sessions → click the first → its full history is restored.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any rendering/restore issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output→DOM | Rendered markdown (must stay rehype-raw-free) |
| frontend→backend | Bearer-authenticated session/quote fetches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Tampering/XSS | StreamingMarkdown + cards | mitigate | Reuse Phase 1 safe ReactMarkdown config; no rehype-raw, no dangerouslySetInnerHTML (T-06-01) |
| T-02-06-02 | Info disclosure | session fetches | mitigate | All session fetches send the Bearer token; backend enforces user scoping (02-03) |
| T-02-06-SC | Tampering | no new npm installs expected | accept | Reuses existing react-markdown; no new deps (if a debounce util is added, it is a legitimacy-gated install) |
</threat_model>

<verification>
- Runnable gate (slice 11): refresh → sessions in sidebar → click → full history restored.
- `cd trading-chatbot/frontend && npm run build` succeeds; human-verify checkpoint approved.
</verification>

<success_criteria>
- POLISH-01 satisfied: sidebar session list + restore, expandable citation cards, ticker chips, distinct QuoteCard, smooth streaming.
</success_criteria>

<output>
Create `.planning/phases/02-production-polish/02-06-SUMMARY.md` when done.
</output>
